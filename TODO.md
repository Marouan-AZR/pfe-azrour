# TODO

- [x] Corriger l’erreur Twig dans `templates/dashboard/operational.html.twig` : accès à `fiche.operation.stockEntries` (propriété inexistante dans `Operation`).
- [ ] Mettre à jour l’affichage des sorties en attente pour récupérer le bon objet (StockExit) ou utiliser une propriété calculée existante.
- [ ] Nettoyer le cache Twig (optionnel mais recommandé) puis recharger la page pour valider.


