{% extends 'base.html.twig' %}
{% block title %}Nouvelle chambre froide{% endblock %}
{% block body %}
<div class="container-fluid">
    <div class="page-header">
        <div class="header-content">
            <h1><i class="bi bi-plus-circle"></i> Nouvelle chambre froide</h1>
            <a href="{{ path('app_cold_room_index') }}" class="btn btn-outline-light"><i class="bi bi-arrow-left"></i> Retour</a>
        </div>
    </div>
    <div class="form-container">
        {{ form_start(form, {'attr': {'class': 'room-form'}}) }}
        <div class="form-section">
            <div class="section-header"><i class="bi bi-snow2"></i><h3>Informations chambre</h3></div>
            <div class="form-grid">
                <div class="form-group">{{ form_label(form.name) }}{{ form_widget(form.name) }}{{ form_errors(form.name) }}<small class="form-hint">Ex: Chambre A, Tunnel 1...</small></div>
            </div>
        </div>
        <div class="form-section">
            <div class="section-header"><i class="bi bi-thermometer-half"></i><h3>Caractéristiques</h3></div>
            <div class="form-grid form-grid-3">
                <div class="form-group">{{ form_label(form.maxCapacityTons) }}{{ form_widget(form.maxCapacityTons) }}{{ form_errors(form.maxCapacityTons) }}</div>
                <div class="form-group">{{ form_label(form.targetTemperature) }}{{ form_widget(form.targetTemperature) }}{{ form_errors(form.targetTemperature) }}<small class="form-hint">Température cible en °C</small></div>
                <div class="form-group">{{ form_label(form.isActive) }}{{ form_widget(form.isActive) }}{{ form_errors(form.isActive) }}</div>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn btn-primary btn-lg"><i class="bi bi-check-lg"></i> Créer la chambre</button>
            <a href="{{ path('app_cold_room_index') }}" class="btn btn-secondary btn-lg"><i class="bi bi-x-lg"></i> Annuler</a>
        </div>
        {{ form_end(form) }}
    </div>
</div>
<style>
.page-header { margin-bottom: 2rem; }
.header-content { display: flex; justify-content: space-between; align-items: center; }
.form-container { max-width: 700px; }
.form-section { background: var(--glass-bg); border: 1px solid var(--glass-border); border-radius: var(--radius); padding: 1.5rem; margin-bottom: 1.5rem; }
.section-header { display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1.5rem; padding-bottom: 1rem; border-bottom: 1px solid var(--glass-border); }
.section-header i { font-size: 1.5rem; color: var(--primary-light); }
.section-header h3 { margin: 0; font-size: 1.1rem; }
.form-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; }
.form-grid-3 { grid-template-columns: repeat(3, 1fr); }
.form-group { display: flex; flex-direction: column; }
.form-group label { color: var(--text); font-weight: 500; margin-bottom: 0.5rem; }
.form-hint { font-size: 0.8rem; color: var(--text-muted); margin-top: 0.25rem; }
.form-actions { display: flex; gap: 1rem; }
</style>
{% endblock %}
